import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
  allHome: {
    width: "100%",
    height: "auto",
    paddingBottom: 200,
    backgroundColor: "rgb(20,20,20)",
  },
}));
